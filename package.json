{
  "name": "@pitch-dog/slide-check",
  "version": "1.1.0",
  "description": "A local-first 16:9 slide pressure and fit check from pitch.dog.",
  "license": "AGPL-3.0-or-later",
  "homepage": "https://pitchdog-slide-check.dog-pitch.chatgpt.site",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bomkino/pitchdog-slide-check.git"
  },
  "bugs": {
    "url": "https://github.com/bomkino/pitchdog-slide-check/issues"
  },
  "keywords": [
    "pitch-deck",
    "slides",
    "presentation",
    "local-first",
    "accessibility"
  ],
  "engines": {
    "node": ">=22.18"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "build": "vite build && node scripts/build-sites-worker.mjs",
    "test:hosting": "node --test tests/hosting.test.mjs",
    "verify": "npm run check && npm run test && npm run build && npm run test:hosting"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.0.0"
  }
}
